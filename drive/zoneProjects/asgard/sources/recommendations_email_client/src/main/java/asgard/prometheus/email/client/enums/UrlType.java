package asgard.prometheus.email.client.enums;

/**
 * Supported URL types.
 *
 */
public enum UrlType {
    /**
     * Email recommendation URL requesting a single recommendation image.
     */
    EMAIL_SINGLE_IMG_REQ,
    /**
     * Email recommendation URL requesting a single product redirection.
     */
    EMAIL_SINGLE_PROD_REQ
}
